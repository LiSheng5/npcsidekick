# 大脑侧代码卫生评审报告（2026-08-25 · 评审员 B · 只读）

> 归档自后台评审 subagent 3498ab17。基线：682 passed。未修改任何文件。

范围与方法：通读 npc/ 全部核心模块、scripts/migrate_memory_cards.py、agent/llm/client.py、agent/executor/retry.py、两个启动 bat；全库 grep 交叉验证。

---

## 一、修了最省心（机械改动，零/低风险）

### 1. 未使用导入 ×3 + 冗余本地导入 ⭐｜低
- world.py:15 deepcopy 无使用；server.py:45 JSONResponse 未使用；npc.py:30 needs_deep_review 仅导入未用
- npc.py:101/132 本地重复导入；scheduler.py:342 `_Opt` 别名冗余

### 2. 已退役 A 机的"幽灵开关"仍在生产配置｜低
- 启动NPC服务器.bat:8 仍 `set NPC_SUBAGENT_A=1`；/api/version 的 `subagent_roles.a` 如实上报"在岗"=假信号

### 3. 商议队列"只产不消"：pop_discussions 生产无调用｜低-中
- server.py:689 只读末条永不弹出 → `_discussions` 无界增长（内存泄漏）；模块头承诺的对话接线从未实现

### 4. CONTINUOUS_VERBS 死常量 + 文档承诺未实现｜中
- taskloop.py:29 定义后生产零引用；book() supersede 循环不看该集合——follow_player 会被任意新落账取消
- 修复二选一：supersede 排除持续型（需拍板）或删常量修正文档（已拍板：新命令打断，删常量）

## 二、格式不一致清单（会破坏下游解析）

### 5. "完成"三种写法、"失败"三种写法 ⭐本类核心｜中-高
| 写法 | 位置 |
|---|---|
| `完成：{desc}` 全角 imp5 | scheduler.py:251,272 |
| `完成: {task}` 半角 imp8 | npc.py:432 |
| `完成: {desc}` 半角 imp8 | server.py:686 |
| `{desc}没做成` 无冒号 imp4 | scheduler.py:262 |
| `没做成: {desc}（{err}）` imp6 | server.py:688 |
| `「{desc}」没办成（{err}）` | taskloop.py:205 |

下游受害：①去重聚合按精确相等永不合并；②_try_recall 原样吐给玩家；③**最锋利**——迁移脚本 R2 以全角+imp≤5 为垃圾签名，scheduler 代执行玩家单时会**把玩家正事完成记录当垃圾删掉**。
修复：EV_DONE/EV_FAIL 单一来源 + 四写入点引用 + 迁移脚本签名兼容。

## 三、双记账问题（taskloop 账本 vs scheduler 自主循环）

### 6. 同一承诺两条账互不销账——GTA 生产已是活线 ⭐最高价值｜高
证据链：book() 镜像落 LEDGER（npc.py:595-605）；scheduler.py:236-238 tick 直接消费 pending_task 全程不碰账本；mod 轮询 dispatch_view 翻 dispatched；无人销账 → 300s reap_zombies 给**已干完的活**补写失败记忆+推商议字幕；反向 mod 认领则双份执行。触发条件已成立（GTA bat NPC_TASK_LOOP=1）。
附加冲突：账本 300s 超时与 scheduler 步进耗时无关联；失败文案双轨致反思记成两种经历。
修复：gate_enabled 时划界（pending_task 不喂轻路径 或 tick 转换回调 LEDGER.settle）+ 第 5 条前缀常量同源。

## 四、重复实现

7. 布尔开关两套判别语义（宽松 not in vs 严格 =="1"）→ 统一 _env_flag
8. env 数值读取样板 ×5 → _env_num 收口
9. importance/count 钳制两处、content 截断只有 API 通道有 → 钳制收口
10. 重试机制两套并行（executor ExponentialBackoff vs client 手写固定延迟），retry.py 宣称的 FixedDelay 未实现 → 划清边界或注入 RetryPolicy
11. world.log 字符串协议三方散布（world f-string 拼装 ↔ server 正则解析 ↔ server 手插字幕行）→ emit_event/parse 配对单点维护

## 五、死代码/封存代码台账

12. A 机封存资产盘点——**判定：有意封存非垃圾，原样保留**：
A_SYSTEM(subagent.py:180)、a_brief(:188)、NPC._a_semantic_block(npc.py:713-738，仅测试引用)、NPC_SUBAGENT_A 读侧警告 —— 均保留供 A/B 实验。
B2 全套与 REVIEW_RETRY_HINT 在役勿动。
附带文档漂移：subagent.py:25-28 头注仍教 A 开关；reviewer.py 头注"三角色 A 审查"建议改称"规则层审查（原 A）"。

## 总结排序

1 导入清理 → 2 幽灵开关 → 3 商议队列接线 → 4 CONTINUOUS_VERBS 拍板 → 5 完成格式统一 → 7/8/9 开关与钳制收口 → 10 重试划界 → 11 日志协议收口 → 12 封存台账(基本不动) → **6 双记账划界(最高价值，正在产生坏数据的热线)**。
