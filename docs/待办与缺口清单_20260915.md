# 待办与缺口清单（2026-09-15）

> 编制：工友｜验收：主控
> 来源：① 外部 brief 的 12 Phase 对照（见 `docs/NPC_RUNTIME_AUDIT.md`）② `docs/全面审查报告_2026-09-10.md` 遗留项
> ③ `.workbuddy/memory/MEMORY.md` 未决项 ④ **本次新发现**（含实测复现）
> 口径：**本次只读核验，未改动任何生产代码。** 所有状态以 2026-09-15 实测/读码为准，不抄旧报告结论。

---

## 0. 三种状态先分清（避免把设计当 bug）

| 标记 | 含义 | 处理 |
|---|---|---|
| 🔴 **真待办** | 遗漏或已复现的缺陷 | 排期修 |
| 🟡 **部分欠账** | 有雏形，缺机制 / 缺守卫 | 补强，不重写 |
| ⚪ **非待办** | 设计如此，或已裁决跳过 | **别再讨论**（见 §6） |

优先序：**P0 = 不拍板就动不了** > **P1 = 影响正确性** > **P2 = 影响可维护性** > **P3 = 卫生**

---

## 1. 阻塞项：等你拍板（P0，共 6 项）

| # | 事项 | 我的建议 | 卡住了什么 |
|---|---|---|---|
| Q1 | 后 3 份文档落点（ROADMAP / WORLD_MODEL / BEHAVIOR_BENCHMARK） | 并入 `docs/NPC大脑架构.md` 编号章节，避免模块清单两处维护 | Phase 2 起的文档产出 |
| Q2 | 新数据（goal / relationship）在 Web Console 的可观测粒度 | API 先出口，复用 Live/Activity 页，**不做新页** | Phase 9 验收方式 |
| Q3 | `examples/village` 形态 | **可跑参考世界**（能进 benchmark），不是教学模板 | Phase 11 |
| Q4 | 世界权威归属（大脑权威 / 游戏端权威） | 文本单机 = 大脑权威；联机 = 游戏端权威、大脑只提议；按有无活跃消费者切换（复用 `_protocol_owns_pending` 语义） | Phase 2 / 5 设计定形 |
| Q5 | ~~三个未提交文件~~ → **已处置一半**：`tests/test_safety_gate.py` 已随 `d69c726` 提交（2026-09-15 晚）；剩 `?? CLAUDE.zh.md` + `?? npc/store_godot/` 待你说去留 | 测试改动方向正确、已提交；另两个我仍不擅动 | 只差这两个就收干净基线 |
| Q6 | Phase 10（事件订阅式）是否砍掉 | **建议砍**：现有 `world.log` + `/api/events` + 账本派发队列已够 benchmark 与 UI 用，无可证收益 | Phase 10 是否排期 |

---

## 2. brief 12 Phase 的剩余工作（P1，按依赖顺序）

已实测判定：**6 已有 / 4 部分 / 2 真缺**。Phase 1（审计）✅ 已完成。

| # | Phase | 现状 | 待做 | 改动面（最小版） |
|---|---|---|---|---|
| P-1 | 2 WorldState | 🟡 80% | 加 `relationships`；`entities`/`objects` 作为 `locations` 的可选扩展键（声明驱动，缺省零差异） | `npc/world.py` 加键与访问器 |
| P-2 | 3 Goal / Motivation | ✅ **模块已落地（2026-09-16）**（原 🔴 真缺） | `npc/goal.py`：`Goal` + `GoalQueue`（六态生命周期 / 前置 BLOCKED / 超期 FAILED / `apply_result` 由 G3 后果推进 / `seed_from_persona` 种子）+ 序列化往返；锚 `tests/test_goal.py` 15 例 | **接线（P-3/G1）尚未做** —— 目前还没有人消费它 |
| P-3 | 4 决策源 | ✅ **已落地（2026-09-16）**（原 🟡 有抽签无目标） | 开关 `NPC_GOALS`（默认关 = 逐字节同旧版）：候选 = `routine ∪ active_goals`（没绑 action 的目标不瞎开工）、按优先级抬权（9→×3 / 5→×1 / 1→×1/3）、整链干完 → 目标 +1（失败不推进）、目标层故障降级不拖垮整帧；`features.flags.goals` 暴露态 | 锚 `tests/test_goal_decide.py` 16 例；**开关打开态全量也 905 通过** |
| P-4 | 5 Action → Consequence | ✅ **G3 已落地（2026-09-16）**（原 🟡 60%） | `npc/action_result.py`：`apply_action_structured()` + `ActionResult`（success / error_code / world_changes / duration_ms / observation），旧签名不破、零行为变化；锚 `tests/test_action_result.py` 12 例 | 新模块 + 测试；**消费点尚未接**（G2 目标进度 / G4 关系 / benchmark 待做） |
| P-5 | 6 反思 → 重规划 | ✅ **已落地（2026-09-16）**（原 🟡 30%） | reflection 条目加**可选** `scope`/`recommendation`（`npc/memory.py::clean_scope/clean_recommendation`；**两者齐备且合法才写** → 老条目零回归；scope 键数超限**整条拒绝** = fail-closed）；`_lesson_factor()` 按 `2**rec` 调权（+1→×2 / −1→×0.5，多命中取绝对值最大）；开关 `NPC_LESSONS`；`features.flags.lessons` 暴露态 | 锚 `tests/test_lessons.py` 32 例 + **A/B 探针 `scripts/probe_lesson_ab.py`**（有经验 → 选该活 157→98，降 38%；关/无经验 → 与基线逐次一致） |
| P-6 | 7 记忆打分 | ✅ **已落地（2026-09-16）**（原 🟡 4/5 因子） | 加第 4 个因子 `goal_relevance`（`_GW=(0.5,3,2,2.0)`）：与**活动目标**的规范词重合度 ×2 加权；开关 `NPC_GOAL_RELEVANCE`（**默认关 = 一字不加、逐字节同分**）；目标文本每 tick 注入记忆层且**不落盘** | `npc/memory.py` + `npc/scheduler.py`（`_sync_goal_terms`）；锚 `tests/test_goal_relevance.py` 11 例 |
| P-7 | 8 Personality | 🟡 只在 prompt | 结构化（risk_tolerance 等）**影响决策权重**而非 prompt；带两种人格 → 不同决策的 A/B | `persona.py` + `scheduler.py` + 测试 |
| P-8 | 9 Relationship | 🟡 接缝已留、数据为 0 | `world["_relationships"]` 由事件驱动增量（2–5 NPC 可重复验证） | `world.py` + 事件消费点 |
| P-9 | 10 Event System | 🟡 轮询式已够 | ⚪ **建议不做**（见 Q6） | — |
| P-10 | 11 Small World | 🟡 等价物已有 | `examples/village/`：声明式 JSON（5 persona + world 声明），**引擎层零游戏词** | 新目录 |
| P-11 | 12 Benchmark | 🟡 **指标化骨架已落地（2026-09-16）**（原 🔴 真缺，与 Goal 真值层并列的两个真缺之一） | `npc/benchmark.py`：结构化结果 + **断言（10 任务全对）** + **退出码（不符预期 → 非零）** + `--json`（**纯 JSON，CI 可直接解析**）+ **自主循环机制指标**（纯规则 N 帧，固定 seed 确定性；含动作计数/交付/失败数） | 锚 `tests/test_benchmark.py` 11 例（含子进程验证 JSON 纯度、人为回归验证退出码真会失败）。**仍待**：`tests/benchmark/` + `tests/e2e/` 目录结构；CI 工作流；「关键 A/B 走真 API 少量」那一层 |

> **最大提醒**：P-5（反思改变行为）与 P-3（决策层读记忆）是**同一件事的两面**。
> 只把 P-5 做成"反思日志"就是白做 —— 必须先在 P-3 打开"决策层读记忆/目标"的口子。顺序不能倒。

---

## 3. 代码层待修（P1 / P2）

### 3.1 本次新发现（审计时实测复现）

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| **T-01** | ✅ **已修（2026-09-15 · `0b6dc49`）** —— 原状：routine 里写 `craft` 抛 `KeyError 'resource'`（`_plan_steps` 对非 `rest`/`say` 项一律走 gather 链 `resource = item["resource"]`）。**执行时实测严重度更高**：异常冒到 `server._tick_loop` 的 `except` 会吞掉**整帧**（该帧落盘/反思/管家/黎明整理/`reap_zombies` 全跳过），且**玩家单也炸**（`_task_from_b2` 产得出 craft 单 → pending_task → 同一处）。修法：① `_plan_steps` 变总函数（认不出 → `None`）② 计划失败也进冷却（否则每帧写一条记忆 = 刷屏）③ 失败文案按原因分开 | 复现命令仍可跑（现返回 `None` 不抛）；回归锚 `tests/test_scheduler.py::TestPlanStepsIsTotal` 10 例（先红后绿，红时全停在 `npc/scheduler.py:163`）；全量 847 → **857** | P1 → **已修** |
| **T-02** | ✅ **已修（2026-09-15 · 方案②）** —— 原状：重启权威 = cast 顺序第一张卡 → 反转顺序即整村回退。修法：取 **`saved_at` 最新的卡**（`npc/server.py::_newest_card_pid`；并列取 cast 先者；时间戳缺失退化文件 mtime；单卡损坏不炸启动） | 回归锚 `tests/test_restart_authority.py` 5 例（先红后绿）；探针复测「反转顺序不再换权威」✓。**边界**：权威跟时间戳不跟内容（手改卡者自负 `saved_at`） | ✅ 已修 |
| **T-03** | ✅ **已修（2026-09-16）** —— 原状：引擎硬编码游戏地名（`walk_to("村庄")`）。修法：**声明驱动** —— 世界声明 `craft_at` 决定"在哪能制作"（**未声明 = 就地制作**），出生点读 `_default_spawn`（未声明 → 第一个地点）；引擎层不再出现任何具体地名 | `npc/world.py`（新增 `craft_station()`；craft 闸门与 `actor_of` 兜底改读声明）+ `npc/npc.py`（craft 步骤走声明）；锚 `tests/test_hygiene_t03_t08.py::TestT03NoHardcodedPlace`（含**虚构地名世界**证明游戏无关） | P2 → ✅ |
| **T-04** | ✅ **已修（2026-09-16）** —— 原状：scheduler 的 routine **永不 craft**（`_plan_steps` 对非 gather 项一律 None）。修法：routine 里的 craft 项现在**真的能编排与执行**（先走到声明驱动的制作台，再出 `{"kind":"craft","recipe":…}` 步骤）；`_describe` / `npc.state` 状态表 / 失败文案 / `SUPPORTED_ROUTINE_ACTIONS` 一并补齐 | `npc/scheduler.py`（`_plan_steps` / `_execute_step` / `_describe` / 状态表 / `_plan_failure_note`）；锚 `TestT04RoutineCanCraft`（含**端到端**：人设写 craft → 跑几帧 → 背包真有产物）。⚠ 既有 `TestPlanStepsIsTotal` 的两处"craft 不可编排"样例已随契约变更改成自定义动作（并在注释里写明原因） | P2 → ✅ |
| **T-05** | ✅ **已修（2026-09-16）** | `npc/npc.py` 里重定义的 `_reflect_rules` 已删 —— 现在**只有 `memory_card` 一处实现**（导入即唯一真相）；锚 `TestT05SingleReflectionRules`（`is` 同一对象 + 源码零 def） | P2 → ✅ |
| **T-06** | ✅ **已处置（2026-09-16）**（按"乙案"保留封存 + 补"无调用点"锚） | `npc/npc.py::_a_semantic_block` 保持封存；docstring 补一句"无调用点由测试钉住"；锚 `TestT06SealedMethodStillSealed`（只扫 `npc/` 生产代码、剥 `#` 注释后判**调用**；谁把它接回生产路径就红。tests/ 里直接调它是正当的，故不扫测试目录） | P2 → ✅ |
| **T-07** | ✅ **已修（2026-09-16）** | 两处裸 `except Exception` 现在都**留痕**（`npc_profile_read_failed` / `npc_discussion_read_failed`，带 npc + error），仍 fail-soft 不卡对话；锚 `TestT07SwallowedExceptionsLeaveATrace`（caplog 抓日志） | P2 → ✅ |
| **T-08** | ✅ **已修（2026-09-16）** | `npc/reviewer.py` 头注重写为"Agent B 任务编译 + 规则层审查"，并写明 A 审查【**已退役 · §22 / 2026-08-25**】、机器封存位置，与 `npc/subagent.py` 对齐；锚 `TestT08HeaderSynced` | P2 → ✅ |
| **T-14** | 🟡 **T-03 的相邻发现**： 里除逻辑路径外还有一张**地点同义词词典**（ 等）—— 同属"游戏数据住在引擎里" |  附近；要动得先定"词典怎么声明化"（世界文件带 ？）→ 属设计决策，等你拍板 | P3 |

### 3.2 审查报告遗留（2026-09-10 未处置，本次逐条复核仍在）

| # | 问题 | 证据 | 级别 |
|---|---|---|---|
| T-09 | `create_npc_server` 闭包仍约 740 行未拆 APIRouter | `npc/server.py:330` → 文件尾 1079（S10） | P3 |
| T-10 | 历史 `npc/store/*.json` 全角「完成：」存量未归一（迁移脚本垃圾签名需持续兼容） | 实测命中 `npc/store/ali_memory.json`、`cang_memory.json`（S5） | P3 |
| T-11 | `httpx` 已是 `openai` 传递依赖，仍列为直依赖；dev 依赖与 `requirements-dev.txt` 重复 | `pyproject.toml:16`（S8） | P3 |
| T-12 | `npc/personas/{ali,cang}.json` 含具体角色名且入库（属 demo 样本，边界案例） | `npc/personas/` 实测含 `ali.json` `cang.json`（S9） | P3 |
| T-13 | ✅ **已修（2026-09-15）**：原状 —— 过时引用 npc.html（该页 2026-09-08 已下架）：死链 docs/index.html:40（改为指向真仓库 + 架构文档）；大脑架构 §15（加"页面已换"提示，并新增 §31 记录下架）；npc/server.py:5 docstring（改指 /console/）。另修了同类残留：PROTOCOL.md:170、web/console/README.md:25、CLAUDE.md/zh | P3 → ✅ |

---

## 4. 文档层欠账（P2 / P3）

| # | 事项 | 证据 | 级别 |
|---|---|---|---|
| **D-01** | ✅ **已订正（2026-09-15）**（原状：实测 857、文档仍写 841）—— 范围比原先列的更广：README:4 徽章 / ARCHITECTURE:412,469 / PROJECT_DELIVERY:18,20,32 / **CLAUDE.md + CLAUDE.zh.md 各 5 处** / 大脑架构 §6 —— 全部统一为 **857** | 2026-09-15 三次全量实测（junitxml）均 857 passed / 0 failed / 0 errors / 0 skipped | ✅ 已订正 |
| D-02 | ~~本次审计结论未进编号章节~~ | ✅ **已完成（2026-09-15 晚，Q1 已拍板）**：结论并入 `docs/NPC大脑架构.md` **§29**（能力边界与三条断层）+ **§30**（演进路线与待拍板）。三份分工固定为：**§29/§30 = 长期结论｜`NPC_RUNTIME_AUDIT.md` = 证据快照｜本清单 = 状态台账** | ✅ |
| D-03 | `MEMORY.md` 未决项需随本次进展收敛（Step 8 Graph 精修仍在） | `.workbuddy/memory/MEMORY.md` 已随 2026-09-15 两次进展收敛（未决项改写、基线改 857） | P3 |

---

## 5. 验证性欠账（P1 —— 「没跑过」也是一种没做）

| # | 欠账 | 为什么重要 |
|---|---|---|
| **V-01** | **未做 30 分钟长跑**（brief 验收 J 项） | 记忆条目 / 向量锚 / 归档文件在长跑下的增长从未实测；只有静态体积护栏 |
| **V-02** | ✅ **已实测（2026-09-15 · `scripts/probe_restart_consistency.py`）** | 正常重启一致 ✓；但**权威 = cast 顺序里第一张有卡的 world，且不看新鲜度** → 反转顺序即整村回退（已复现）。**结论：T-02 不是纸面担忧** → **同日已按方案②修复**（见 T-02 行） |
| **V-03** | 真实联机端到端未跑 | 缺 GTA mod 侧，属外部依赖 |
| **V-04** | ✅ **已补（2026-09-16）**（原状：脚本级雏形、缺指标化骨架） | `npc/benchmark.py` 现在有：**断言**（10 任务符合预期，含 3 个受阻场景"优雅失败"）、**退出码**（不符预期 → 非零）、`--json`（**纯 JSON**，CI 可消费）、**自主循环机制指标**（`scheduler.tick_round` 纯规则 N 帧，固定 seed 确定性）；`llm_calls` 恒 0（CI 无需 key）。**仍未做**：GitHub Actions 工作流本身（见 D-04 建议）；「关键 A/B 走真 API 少量」那一层 | 这是"加了 Memory / Reflection 之后 NPC 到底有没有变聪明"的证据来源 —— brief 最终验收 I 项；P-11 已从 🔴 真缺降为 🟡 |
| V-05 | 前端契约核对基于静态比对，未起真实服务联调 | 审查报告已声明 |

---

## 6. ⚪ 明确「不是待办」（设计如此 / 已裁决，防重复讨论）

| 项 | 判定依据 |
|---|---|
| 规则模式（无 LLM）不记对话历史 | **设计如此**（`tests/test_npc.py::test_rules_mode_no_history` docstring）；2026-09-08 曾误判为 bug 改坏后回滚 |
| 自主日常不记入任务账本 | 评审已定性**非缺陷**：自主日常不是玩家承诺，无需账本 |
| 引擎不抢跑 `pending_task`（M2 划界） | 设计（`scheduler.py:43`），已修历史双记账 B6，有回归锚 |
| 记忆卡内 `world.log` 只留 500 条 | 有意护栏（防 2.3MB 卡）；归档留全量、只追加不删除 |
| `archived` 条目只增不减 | **红线优先**（用户裁决），不物理删除 |
| I5 无鉴权（仅绑 127.0.0.1 + Origin 白名单） | 用户裁决"按不动"（本机工具够用） |
| I6 推送 11 个提交 | ✅ **已完成**：41 提交，`master...origin/master` = `0 0` |
| I3 外层 `0_先读我.txt` 地图失效 | ✅ 已订正 |
| I2 文档基线订正 | ✅ 已订正为 841（但现又过期 → 见 D-01） |
| I4 重型可选依赖 | ✅ 已移入 `[project.optional-dependencies] memory` |
| Rules-mode talk pipeline 无状态 | **by design**（ArgOS 同源纪律） |

---

## 7. 环境 / 仓库杂项（P3，需你批准才动）

| # | 事项 | 实测 |
|---|---|---|
| E-01 | 外层 4 个遗留目录占空间，**在 git 仓库之外、不入库**：`.git_backup_20260828_2045`、`.git_badrefs_20260828`、`_verify_bak`、`snapshot_20260828` | `ls D:\NPCSidekick` 命中全部 4 个。**属删除动作，我不擅动，等你点名** |
| E-02 | **已处置（2026-09-15）**：CLAUDE.zh.md → 已加进 .gitignore（防误提交，文件本机保留）；npc/store_godot/ → **仍未被 ignore**，建议加 npc/store_*/，等你点头 | git status 复核 |

---

## 8. 建议执行顺序

```
第 0 步  拍板 §1 的 6 项（Q1-Q6）           ← Q1-Q4/Q6 仍未拍板（不拍板不动对应代码）
第 1 步  ✅ **已完成（2026-09-15 晚）**：修 T-01 + safety_gate 测试加固提交（`0b6dc49` / `d69c726`）；基线 847 → 857 全绿
第 2 步  ✅ **已完成（2026-09-15）**：V-02 实测（§10）→ **T-02 已按方案②修复**（`_newest_card_pid`）；Q4「联机时谁是权威」仍待拍板
第 3 步  ✅ **已完成（2026-09-15）**：D-01 基线订正（→ 857）+ 版本号统一 3.2.0 + T-13 过时引用清理 —— 详见 §9
第 4 步  ✅ **三根柱子全齐（2026-09-16，905 passed）**：G3 ActionResult（`npc/action_result.py`）+ G2 Goal 真值层（`npc/goal.py`）+ **G1 决策源扩展**（`npc/scheduler.py`，开关 `NPC_GOALS` 默认关）—— **闭环首次合拢**（目标 → 决策 → 后果 → 目标进度）。⚠ 仍待：让**记忆/反思**也进决策（G1 的另一半），即第 5 步的 Phase 6 反思结构化
第 5 步  ✅ **已完成（2026-09-16，948 passed）**：P-5 反思结构化 + A/B（`NPC_LESSONS`）+ P-6 `goal_relevance` 因子（`NPC_GOAL_RELEVANCE`，关时逐字节同分）。⚠ 但 TD1「记忆 → 决策」只合拢了**目标 + 结构化反思**两路；**任意检索记忆 / 画像 / 管家产出仍未进决策**
第 6 步  P-7 人格参与决策 + P-8 关系数据（各带 A/B）
第 7 步  **进行中**：✅ P-11 benchmark 指标化（在 `npc/benchmark.py` 之上，保留 10 任务表与 `run_task` 路径）；⬜ P-10 `examples/village`
第 8 步  V-01 30 分钟长跑验收
顺手     T-03 ~ T-12（卫生项，改到哪块顺手清哪块）
```

**两条纪律**（沿用项目既有习惯）：
1. **每步改完立刻跑相关测试**，全量基线（**857**）只升不降；收尾用 junitxml 解析，**别信终端尾部**（safe-delete shim 会吞摘要；junitxml 也别写系统 Temp）。
2. **每个新机制都带开关**，关 = 与旧版逐字节一致 —— 这是本项目 8 个 `NPC_*` 开关的既有契约。

---

---

## 9. 本轮对齐与清理（2026-09-15 晚 · 用户："对齐并稳定相关功能，清理多余或冗余的文档"）

**纪律**：先只读核查 → 能客观判定的直接改 → **删除/移动类一律先问**。改动后全量 **857 passed** 零回归。

### 9.1 已对齐（逐条实测）

| 类别 | 处 | 处置 |
|---|---|---|
| 基线数字 | 见 D-01 | 统一 `857`（其中 `大脑架构` §6 的 588 是**第二次提交补上**的 —— 首个提交信息里声称已改、实际漏改，已如实记录；§14 的 588 属该章历史快照，保留） |
| 版本号 | `pyproject.toml` / `npc/__init__.py`（`0.1.0` →）/ README 徽章 / `ARCHITECTURE.md` 标题 | 统一 **3.2.0**（`npc/server.py` 的 `BRAIN_VERSION="2026.08.24"` 是另一套日期式握手版本，未动） |
| 旧 Web 引用 | `npc/server.py` docstring / `PROTOCOL.md` / `web/console/README.md` / `docs/index.html`（死链 `npc.html` + `github.com` 根地址）/ `CLAUDE.md` + `CLAUDE.zh.md` | 一律改指 `/console/`；`大脑架构` 新增 **§31** 补上"旧 Web 下架"这段缺失的历史，§15 加"页面已换"提示 |
| 项目路径 | `参考_agent好货/README`（5 份任务书当日晚些被用户拍板移出，见 §9.2） | `1_Dagent` → `1_NPCSidekick`（保留旧名注记） |
| 契约矛盾 | `游戏接入.md:8` 的 `pip install npcsidekick` **vs** README「尚未发布 PyPI」 | 改为本地安装 + 注明未发 PyPI |
| 契约缺口 | `docs/API.md` 只覆盖 8 条端点（实为 40 条） | 新增 **§9 完整端点索引（40 条）** + 补全错误码 409/422/429/500/503 |
| 状态过期 | `日志分档_设计稿` 标题标"待审"（正文已实施）/ `WEB_CONSOLE` 指引停在 §12 | 改标注；补 **§13 补记**（Live / Activity / Playground / Settings） |

### 9.2 已清理（**移出仓库目录**，可反悔 —— 全在 `D:\NPCSidekick\_已归档_20260915\`）

| 项 | 判定依据 |
|---|---|
| `docs/参考_agent好货/` 的 6 份 .py | 与 `agent/` 里**逐字节相同**（md5 已核对）；只留 README 当地图，副本移出 |
| `docs/任务书_02~06`（5 份 / 346 行） | **用户拍板删除**（2026-09-15）：2026-08 的执行指令，成果已全部进 `大脑架构` §23~§28；移出仓库、可反悔 |
| `.claude/analysis`、`checkpoints`、`sessions`、`state.md` | 本机 AI 分析产物（`state.md` 还停在 2026-07-01 的 v3.0 时代），且已 gitignore；**保留** `settings.json` 与 `skills/`（那是配置） |
| `api_key_zen.txt` | 🔴 明文 API key（已 gitignore、未入库）→ 移出仓库目录 |

### 9.3 仍未做 / 待你定

| # | 事项 | 卡在哪 |
|---|---|---|
| 1 | `docs/demo.mp4`（21MB，已入库）**外链化后移除** | **`gh` 未登录**（`gh auth status` = not logged in），我无法上传到 Release；需你 `gh auth login`，或给我一个外部直链（Release / 网盘都行） |
| 2 | `npc/store_godot/`（Godot 线运行时记忆卡，**目前未被 ignore**，可能被误提交） | 建议 `.gitignore` 加 `npc/store_*/`（与既有 `npc/store/`、`npc/store_test/` 同族）；等你点头 |
| 3 | ~~`任务书_02~06`~~ → ✅ **已删（用户拍板 2026-09-15，移出仓库）**；`评审/*` 与三份 `*_方案稿.md` **建议保留**（都有日期且自标"已实施"，成果已进 `大脑架构` §23~§28） | 任务书已处置 |

---

## 10. V-02 实测：重启一致性（2026-09-15 · 只测不改）

复现：`python scripts/probe_restart_consistency.py`（临时 store，跑完即删；只读，不动生产代码）

| 场景 | 结果 |
|---|---|
| 正常重启（两张卡都新鲜） | ✅ 世界逐字段一致（tick / delivered / 位置 / 背包 / log 长度），不丢状态 |
| 盘上两张卡的 world | ✅ 逐字段相同（同一进程共享同一 world，各卡序列化的是同一份内存） |
| 非首位 NPC 的卡更旧 | ✅ 被丢弃，不污染权威 |
| **反转角色表顺序** | ❗ **权威换人 → 整村回退**：tick 12→1、`delivered` 木材 5→0、该 NPC 位置 村庄→矿洞 |

**所以 TD2 不是纸面担忧，而是「隐式 + 顺序敏感」的权威**：`load_village`（`npc/server.py:283-296`）取
**cast 迭代顺序里第一个有记忆卡的 NPC** 的世界当权威，丢弃其余卡的 world，**也不看 `saved_at` 新鲜度**。
触发条件很日常：加/删/改名人设文件、目录排序变化，或那个 NPC 恰好长期只在「转换点」落盘（卡比别人旧）→ 整村退档。
现有测试**没覆盖这条语义**（`load_village` 只测了「适配器世界覆盖」与「人设优先于旧卡」）。

**修法三选一（请拍板；顺带定 §30.3 的 Q4 世界权威归属）**：
① **最小**：把「第一张卡胜出」写成显式契约 + 补回归锚（钉住语义，成本最低，但不解决回退）；
② **按新鲜度**：`saved_at` 字段现成 → 选最新的一张卡当权威（能挡住陈旧回退，仍是「借 NPC 卡」的架构）；
③ **独立世界存档**：world 单独落一份 `world.json`，谁权威 = 世界文件本身（与 Q4 配套，最干净但改动最大）。


---

**后续（同日）：T-02 已修** —— `load_village` 的权威改为 **`saved_at` 最新的那张卡**（方案②），顺序无关；回归锚 `tests/test_restart_authority.py` 5 例（先红后绿）；探针复测 ④「反转角色表顺序」不再换权威 ✓。
边界如实记录：⑤「手改内容 + 伪造最新 `saved_at`」的卡**仍会被采纳**（权威跟时间戳，不跟内容）。
全量 **857 → 862 passed / 0 failed / 0 errors / 0 skipped**。

---

*本清单为只读核验产出（编制时未改任何生产代码）；自 2026-09-15 晚起随时间推进**就地更新状态**，不要另起一份。*
