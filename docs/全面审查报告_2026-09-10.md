# NPCSidekick 全项目审查报告（2026-09-10 · 只读）

> 审查范围：全项目体检 —— 安全合规 / 测试基线 / 文档-代码一致性 / 后端核心逻辑 / 前端 Web Console。
> 方法：三路并行只读深挖 + 独立证据核验（junitxml 解析、`git ls-files`/`check-ignore`、`tsc` 构建、逐条抽查 file:line）。
> 本次**未改动任何生产代码**。
> 基线对照：上一份评审为 `docs/评审/2026-08-25_大脑侧代码评审/`（当时基线 682）。

---

## 0. 一句话结论

**项目处于健康状态**：测试 841 全绿、前端可构建且无类型错误、密钥无泄露、游戏无关红线守住。
没有「阻塞」级问题。**欠账集中在三类**：① 双记账在自主循环侧仍未闭环（需运行时验证）；② 文档里的基线数字/目录地图大面积过期；③ 10 个提交未推远端。

| 级别 | 数量 | 摘要 |
|---|---|---|
| 阻塞 | 0 | — |
| 重要 | 6 | 双记账残留、文档基线过期、外层地图失效、依赖必装过重、无鉴权形态、10 提交未推 |
| 建议 | 12 | 通用层残留游戏地名、裸 except、craft 能力缺口、封存代码未迁、死链、冗余依赖等 |

---

## 1. 测试基线（独立证据 ✅）

用系统 python 跑全量并**解析 junitxml**（不依赖终端摘要，规避 safe-delete shim 吞摘要的老坑）：

```
TOTAL tests=841  failures=0  errors=0  skipped=0     （耗时 1m04s）
```

- 命令：`python -m pytest -q --junitxml=D:/NPCSidekick/review_junit.xml`
- 与记忆口径一致（2026-09-09 起 841）。**基线成立，无回归。**

---

## 2. 健康面（做得好的，别改坏）

| 项 | 证据 |
|---|---|
| 前端可构建、零类型错误 | `npm run build`（`tsc --noEmit && vite build`）EXIT=0，204 模块，4.95s |
| 前端 24 个 API 调用全部有后端路由 | `web/console/src/api/client.ts:52-122` ↔ `npc/console_api.py` / `npc/server.py` 全覆盖，无 404 风险 |
| 7 项导航全部实现且可达 | `web/console/src/App.tsx:10-18` NAV 全 `ready:true`，`:56-62` 全部条件渲染，无"规划中"死链 |
| 游戏无关红线守住 | `web/console/src` grep `gta/洛圣都/侠盗` 零命中；关系类型/动作名 API 驱动（`console_api.py:183/202/363-399`） |
| 密钥加密 + 脱敏 | DPAPI `CryptProtectData`（`npc/secrets.py:41-60`），API 只回 `masked_key`（8 圆点+末4位，`secrets.py:84-89`） |
| 无明文密钥入库 | `api_key_zen.txt`、`npc/config/providers.enc` 均被 `.gitignore` 忽略且**从未被提交**（`git log --all -- api_key_zen.txt` 为空） |
| 记忆写入三态正确 | `added/merged/rejected`（`console_api.py:437-450`），前端 `MemoryPage.tsx:307-309` 分别处理 |
| 无 TODO/FIXME 残留 | 全仓 grep 为 0 |
| 记忆卡体积护栏生效 | `npc/store/ali_memory.json` 130KB（曾涨到 2.3MB）；`memory_card.py:116` 防抖 + `:149` world.log 截断 500 条 |

### 老欠账「已还」清单（对照 2026-08-25 评审）

| 老问题 | 现状 | 证据 |
|---|---|---|
| F9 三道门序列重复两份 | ✅ 已合流 | `npc/book.py:20` `guarded_book` 单入口 |
| F11 save() 全量重写整卡 | ✅ 防抖 + 截断 | `memory_card.py:116`（无变化跳过写盘）、`:149`（只留最近 500 条 log） |
| F12 配置双轨 / 布尔语义混乱 | ✅ 已统一 | `agent/config_flags.py:13` `_TRUTHY={1,true,yes,on}`、`:16` `env_flag`、`:24` `env_num` |
| B5 完成/失败文案多写法 | 🟡 记忆卡侧已统一 | `memory.py:37-38` `EV_DONE/EV_FAIL` 单一来源 |
| O1 开关生效态不可观测 | ✅ 已补 | `npc/server.py:842-854` `features.flags` 含 safety_gate/scheduler_queue/memory_dedup/task_loop_gate/approval_policy/模型名/tick_interval |
| R1 `pip install .` 缺 fastapi | ✅ 已补 | `pyproject.toml:11-13` 已含 fastapi/uvicorn/edge-tts |
| F1 NPC 上帝类 | 🟡 部分拆 | `npc/npc.py` 754→459 行，拆出记忆/对话/LLM 装配/book |

---

## 3. 重要项（建议尽快处理）

### I1. 双记账：原 B6 已由 M2 划界修复，缺的是回归锚 🟡（同日已补）
- **原 B6（引擎与外部消费者抢 `pending_task` → 双份执行 + 已完成被误判僵尸）已修**：
  `npc/scheduler.py:43 _protocol_owns_pending()` 在 `NPC_TASK_LOOP=1` 时返回 True，
  `_tick_one`（`scheduler.py:252`）便不消费 `pending_task` —— 玩家单独家归 mod，引擎不抢跑。
- 剩余「自主日常写记忆不记账」（`scheduler.py:267/278`）**不是缺陷**：自主日常不是玩家承诺，无需账本。
- 真正的缺口是**没有回归锚**（原评审未留；`test_taskloop_reinforce.py` 只覆盖 A/B/C）→
  同日已补 `tests/test_ledger_boundary.py`（6 例），并做变异验证（打断划界 → 断言变红）。
- ⚠️ 修正说明：本节初稿把它判为「未闭环的热线」，经读码复核**定性偏重**，已按事实校准。

### I2. 文档基线数字过期（公开仓库可见）✅ 同日已订正
| 文件 | 原值 | 处置 |
|---|---|---|
| `README.md:4` | 徽章 `tests-440 passed` | ✅ 已改 **841** |
| `ARCHITECTURE.md:412,469` | 272 tests | ✅ 已改 **841**（`:470` 耗时 10.5s→~64s 同步） |
| `ARCHITECTURE.md:491` | 272 tests | ⏸ **有意保留**（2026-07 版本历史行，加注「引擎层，历史快照」） |
| `PROJECT_DELIVERY.md:18,30` | 321 | ✅ 已改 **841**（表下加口径注记） |
| `CLAUDE.md:29,31,327,364,378` | 321 | ✅ 已改 **841**（本地文件，`.gitignore:21` 忽略） |

口径来源已核实（`.claude/sessions/SESSION_LOG.md:124,145,180`：272 → 321 → … → 841）。
**避开的陷阱**：`docs/记忆卡治理_方案稿.md:23` 的 `251/272` 是**行号**、`WEB_CONSOLE:271` 的 `count=321` 是 **safe-delete 计数** —— 均非测试数，未动。

### I3. 外层「三兄弟地图」已失效 🔴
`D:\NPCSidekick\0_先读我.txt`：
- `:3,6` 写大脑系统 = `1_Dagent\`，**实际是 `1_NPCSidekick\`**；
- `:16-18` 写 `robot\`（宇树狗实验田，`127.0.0.1:8766`，`python -m pytest robot/tests`），**外层已无 robot 目录**。
新机器/新会话接手会照着找不到东西。

### I4. 重型可选依赖被列为必装 ✅ 同日已处置
`pyproject.toml` 的 `chromadb` / `tiktoken` 已移入 `[project.optional-dependencies] memory`（`pip install ".[memory]"` 取回）；`requirements.txt` 同步移入可选注释段。
- **安全前提已实测**（阻断两者导入后运行）：`HAS_CHROMADB/HAS_TIKTOKEN=False`，`count_tokens` 走粗估、`VectorStore()` 仍可构造并打出友好提示，**无 ImportError**。
- 全仓仅 `agent/memory/vector_store.py:19`、`token_counter.py:11` 两处 import，均在 `try` 块内。

### I5. 无鉴权（当前设计预期，但缺护栏）🟡
- 仅绑定 `127.0.0.1`（`npc/bootstrap.py:119`），Origin 白名单 `server.py:68` `_ALLOWED_ORIGIN_PREFIXES`，无 token。
- 纯本机工具合理；**但若将来放开到 `0.0.0.0`（如局域网/远程调试）会直接裸奔**。建议在 README/代码注明"仅限本机"，或预留 token 开关。

### I6. 10 个提交未推远端 🟡
- `master` 分支，`git status -sb` → `ahead 10`（记忆里写的 6 已过期，实测 10）。
- 未推内容含**整个 Web Console**（Step 4~9）。本机无 GitHub 写凭据。建议尽早补凭据推送，否则本地一旦损坏损失大。

---

## 4. 建议项（优化，不影响正确性）

| # | 问题 | 证据 |
|---|---|---|
| S1 | 通用层残留游戏地名：`walk_to("村庄")` 硬编码 | `npc/npc.py:258-259`（轻微违反 game-agnostic 红线） |
| S2 | 商议队列消费点裸 `except: pass` 吞异常 | `npc/talk_pipeline.py:150-156` |
| S3 | craft 能力只存在于 `run_task`（已脱离热路径），scheduler 的 routine 永不 craft | `npc/npc.py:256-262` vs `npc/scheduler.py:_plan_steps` |
| S4 | §22 封存方法 `_a_semantic_block` 仍在 `npc.py:431`，未迁独立 legacy 文件 | `npc/npc.py:431`（features 位已中性化 `server.py:832`） |
| S5 | 历史 `npc/store/*.json` 全角「完成：」存量未归一（迁移脚本垃圾签名需持续兼容） | `npc/memory.py:35-36` 注释联动 |
| S6 | `docs/index.html:40` 死链 `<a href="npc.html">`；`docs/NPC大脑架构.md:422,428` 仍提 `npc.html` 调试台 | 该页 2026-09-08 已下架（`tests/test_server_console.py:132` 断言 404） |
| S7 | `npc/server.py:5` docstring 仍写"自动打开 `…:8765/npc.html`" | `npc/server.py:5` |
| S8 | 冗余依赖：`httpx` 已被 openai 传递依赖；dev 依赖与 `requirements-dev.txt` 重复 | `pyproject.toml:22-28`、`requirements-dev.txt:4-7` |
| S9 | `npc/personas/ali.json`、`cang.json` 含具体角色名且入库（边界案例，属 demo 样本） | `git ls-files npc/personas` |
| S10 | `npc/server.py` `create_npc_server` 闭包仍约 740 行（`server.py:330`→文件尾 1079），main 已抽走但未分 APIRouter | `npc/server.py:330` |
| S11 | `npc/subagent.py:25-28` 头注仍教 A 开关；`reviewer.py` 头注"三角色 A 审查"未改名 | 原评审 F12 附带项，未清 |
| S12 | 外层遗留目录占空间：`.git_backup_20260828_2045`、`.git_badrefs_20260828`、`snapshot_20260828`、`_verify_bak`（**在 git 仓库之外，不入库**） | `ls D:\NPCSidekick` |

---

## 5. 建议行动顺序

1. **立即（低成本、高收益）**：I2 文档数字批量订正 → I3 更新 `0_先读我.txt` 地图 → S6/S7 清死链与过时注释。
2. **短期**：I6 补 GitHub 凭据推送 10 个提交；I1 加双记账端到端用例（先测后改）；I4 依赖分层。
3. **中期**：S3 craft 能力缺口或明确冻结 `run_task`；S1 通用层去游戏地名；I5 鉴权护栏。
4. **顺手**：S2/S4/S5/S8/S9/S11/S12。

---

## 6. 同日跟进（2026-09-10，本次执行）

| 动作 | 结果 |
|---|---|
| 文档基线数字订正 | README / ARCHITECTURE(412,469,470) / PROJECT_DELIVERY / CLAUDE.md → **841**；ARCHITECTURE:491 历史行**有意保留**并加注 |
| I1 复核 | 读码确认 **M2 划界已修复原 B6**，定性校准（见 I1 节）；补回归锚 `tests/test_ledger_boundary.py`（6 例） |
| 测试基线 | **847 passed / 0 failed / 0 errors / 0 skipped**（原 841 + 新增 6，junitxml 独立解析） |
| 变异验证 | 人为打断 `_protocol_owns_pending` → 新锚立即变红，证明测试**有牙**（非恒真） |
| 临时文件 | 已清理（junitxml ×2 / 订正脚本） |
| I3 外部地图 | ✅ `0_先读我.txt` 全量订正（`1_NPCSidekick`；robot → `D:\ArgOS`）；8766 端口与 `/api/command` 核实后保留 |
| I4 依赖分层 | ✅ `chromadb`/`tiktoken` → `[project.optional-dependencies] memory`；缺依赖降级已实测 |
| 首次提交 | `4303a74`（5 文件）；`master` ahead 10 → **11** |

未动（留待拍板）：I5 鉴权（仅绑 `127.0.0.1` + Origin 白名单，本机工具够用）、I6 推送 11 个提交（本机无 GitHub 写凭据）。

## 附：本次未验证 / 需运行时确认

- I1 已完成读码复核 + 回归锚；**仍未跑真实 GTA 联机端到端**（缺 mod 侧），属外部依赖。
- 前端契约核对基于静态比对，未起真实服务做端到端联调。
- `web/console/node_modules` 已存在，构建为本地复现。

*报告生成：只读审查，未修改任何生产文件。同日跟进：仅改文档数字 + 新增一个测试文件。*
